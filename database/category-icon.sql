-- =====================================================================
--  افزودن ستون آیکون به دسته‌بندی‌ها (برای نمایش در مگا منو هنگام هاور)
--
--  این فایل را یک‌بار در phpMyAdmin → Import اجرا کنید.
--  (MySQL از ADD COLUMN IF NOT EXISTS پشتیبانی نمی‌کند؛ اگر ستون از قبل باشد
--   خطای «Duplicate column» می‌دهد که بی‌خطر است.)
-- =====================================================================

ALTER TABLE categories
  ADD COLUMN icon VARCHAR(255) DEFAULT NULL COMMENT 'آیکون کوچک برای نمایش در مگا منو' AFTER image;
