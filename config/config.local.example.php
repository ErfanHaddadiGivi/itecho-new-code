<?php
/**
 * نمونه فایل تنظیمات محلی.
 *
 * روش استفاده:
 *   ۱. از این فایل یک کپی بگیرید.
 *   ۲. نام کپی را به config.local.php تغییر دهید.
 *   ۳. اطلاعات دیتابیس هاست خود را وارد کنید.
 *
 * این فایل حاوی رمز است و نباید در گیت قرار بگیرد.
 */

return [
    // در هاست نهایی حتماً false بماند
    'debug' => false,

    'db' => [
        'host' => 'localhost',

        // در cPanel معمولاً نام دیتابیس و کاربر با پیشوند حساب شما ساخته می‌شود
        // مثال: mysite_itecho  و  mysite_admin
        'name' => 'itecho',
        'user' => 'root',
        'pass' => '',
    ],

    // ⚠️ فقط برای توسعه محلی: آدرس درگاه را عوض می‌کند تا بتوان چرخه پرداخت
    // را بدون اتصال به زرین‌پال آزمایش کرد. در هاست واقعی این خط را ننویسید.
    // 'zarinpal_base_url' => 'http://127.0.0.1:8090',

    // معمولاً لازم نیست پر شود؛ خودکار تشخیص داده می‌شود.
    // فقط اگر سایت پشت CDN یا پروکسی است و آدرس درست تشخیص داده نشد پر کنید:
    // 'base_url' => 'https://itecho.ir',

    // ─────────────────────────────────────────────────────────────────
    // همگام‌سازی محصولات از گوگل‌شیت (api/sheet-sync.php)
    // یک توکن تصادفی و طولانی بسازید و دقیقاً همین مقدار را در اسکریپت
    // گوگل‌شیت هم قرار دهید. برای ساختن توکن می‌توانید این دستور را اجرا کنید:
    //     php -r "echo bin2hex(random_bytes(32));"
    // تا وقتی این خط پر نشود، نقطه پایانی هر درخواستی را رد می‌کند.
    // 'sheet_sync_token' => 'REPLACE_WITH_A_LONG_RANDOM_TOKEN',
];
