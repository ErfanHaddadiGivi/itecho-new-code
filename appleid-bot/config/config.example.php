<?php
/**
 * تنظیمات ربات اپل‌آیدی — نمونه.
 *
 * ۱) این فایل را کپی کنید و نامش را config.php بگذارید.
 * ۲) مقادیر را با اطلاعات واقعی پر کنید.
 * ۳) config.php هرگز داخل گیت نمی‌رود و با .htaccess از دسترسی وب مسدود است.
 *
 * ⚠️ کلید رمزنگاری را یک‌بار بسازید و هرگز عوض نکنید (وگرنه دادهٔ رمزشده
 *    قابل بازگشایی نیست). برای ساخت یک کلید امن:
 *        php -r "echo base64_encode(random_bytes(32)).PHP_EOL;"
 */

return [
    // --- دیتابیس ---
    'db' => [
        'host'    => 'localhost',
        'name'    => '',
        'user'    => '',
        'pass'    => '',
        'charset' => 'utf8mb4',
    ],

    // --- پیام‌رسان (بله) ---
    // آدرس پایهٔ API. برای «بله» همین مقدار پیش‌فرض درست است.
    //   بله:    https://tapi.bale.ai/bot
    //   تلگرام: https://api.telegram.org/bot   (اگر روی تلگرام خواستید)
    'api_base_url' => 'https://tapi.bale.ai/bot',

    // توکن رباتی که از بخش ساخت ربات بله می‌گیرید
    'bot_token' => '',
    // یک رشتهٔ تصادفی برای امنیت وب‌هوک. مثال ساخت:
    //   php -r "echo bin2hex(random_bytes(24)).PHP_EOL;"
    // این مقدار به دو روش بررسی می‌شود (هرکدام که پیام‌رسان پشتیبانی کند):
    //   ۱) هدر X-Telegram-Bot-Api-Secret-Token (اگر پیام‌رسان بفرستد)
    //   ۲) پارامتر آدرس وب‌هوک: webhook.php?s=<همین مقدار>
    'webhook_secret' => '',

    // --- امنیت داده ---
    // کلید ۳۲بایتی AES-256 به‌صورت base64 (خروجی دستور بالا).
    'encryption_key' => '',

    // --- ادمین‌های اولیه (بوت‌استرپ) ---
    // شناسهٔ عددی تلگرام ادمین‌ها. علاوه بر این، جدول admins هم خوانده می‌شود.
    // شناسهٔ خود را با ربات @userinfobot بگیرید.
    'admin_ids' => [
        // 123456789,
    ],

    // --- عمومی ---
    'timezone' => 'Asia/Tehran',
    'debug'    => false,
];
