# ربات اپل‌آیدی آیتکو (پیام‌رسان بله)

ربات فروش اپل‌آیدی ریجن آمریکا روی **پیام‌رسان بله** + مدیریت کامل داخل بله،
به‌علاوهٔ **پورتال وب** روی سایت که با همین ربات یک دیتابیس مشترک دارد.
PHP خام + PDO + MySQL/MariaDB — **بدون Composer، بدون فریم‌ورک، بدون SSH**.
مناسب هاست اشتراکی cPanel با Webhook و Cron.

> API بله با تلگرام سازگار است؛ فقط آدرس پایه فرق دارد (در config).
> اگر خواستی روی تلگرام اجرا کنی، کافی است `api_base_url` را عوض کنی.

---

## ⭐ نصب سریع (توصیه‌شده) — ویزارد `install.php`

مثل نصاب وردپرس، بیشتر کارها خودکار انجام می‌شود:

1. کل پوشهٔ `appleid-bot/` را روی هاست آپلود کن.
2. توکن رباتِ **بله** را آماده داشته باش.
3. در مرورگر باز کن: `https://YOUR_DOMAIN/appleid-bot/install.php`
4. ویزارد خودش:
   - نیازمندی‌های سرور و **دسترسی نوشتن** را چک می‌کند،
   - **اتصال دیتابیس** را تست می‌کند،
   - `webhook_secret`، `encryption_key` و **رمز راه‌اندازی** را **خودکار می‌سازد**،
   - `config/config.php` را می‌نویسد و **جدول‌ها را می‌سازد**،
   - **وب‌هوک را خودش ست می‌کند**.
5. در پایان یک **رمز راه‌اندازی** نشان می‌دهد؛ در بله به ربات بفرست:
   ```
   /claim <رمز راه‌اندازی>
   ```
   حالا ادمین شدی. سپس `/setcard`، و در آخر `/finishsetup` برای قفل‌کردن ثبت ادمین.
6. **فایل `install.php` را از روی هاست پاک کن.**

اگر بعداً دوباره `install.php` را باز کنی، به‌جای نصب، یک **گزارش اعتبارسنجی**
(چک دیتابیس، جدول‌ها، کلید رمز، وب‌هوک، ادمین) نشان می‌دهد.

### 🔧 نصب ناقص؟ دکمهٔ «تعمیر»
اگر گزارش اعتبارسنجی برای «همهٔ جدول‌ها ساخته شده» یا «حداقل یک ادمین فعال»
علامت ✗ نشان داد (مثلاً چون هاست، فایل SQL چند‌دستوری را کامل اجرا نکرده)،
همان‌جا دکمهٔ **«تعمیر نصب»** را بزن. بدون حذف `config`:
- جدول‌های ناقص را می‌سازد (اجرای هر دستور جداگانه و idempotent)،
- یک **رمز راه‌اندازی جدید** می‌دهد تا با `/claim <رمز>` در بله ادمین شوی،
- اگر تیک «وب‌هوک را دوباره ثبت کن» را بزنی، وب‌هوک را هم دوباره ست می‌کند.

> Cron را هم یادت نرود (بخش ۶ پایین).

---

## راه‌اندازی دستی (اگر نمی‌خواهی از ویزارد استفاده کنی)

### ۱) ساخت ربات
- در «بله» ربات بساز و توکن را بردار (بخش ساخت ربات بله).
- شناسهٔ عددی ادمین(ها) را بگیر (مثلاً با رباتی که آی‌دی عددی می‌دهد).

### ۲) دیتابیس
- یک دیتابیس MySQL بساز.
- فایل `sql/schema.sql` را در **phpMyAdmin → Import** اجرا کن.
  (سه محصول و سه نوع ضمانتِ نمونه هم ساخته می‌شود؛ بعداً قیمت/فعال‌بودن را عوض کن.)

### ۳) تنظیمات
```bash
cp config/config.example.php config/config.php
```
سپس `config/config.php` را پر کن:
- `db` → اطلاعات دیتابیس
- `bot_token` → توکن BotFather
- `webhook_secret` → یک رشتهٔ تصادفی:
  ```bash
  php -r "echo bin2hex(random_bytes(24)).PHP_EOL;"
  ```
- `encryption_key` → کلید ۳۲بایتی (یک‌بار بساز، **هرگز عوض نکن**):
  ```bash
  php -r "echo base64_encode(random_bytes(32)).PHP_EOL;"
  ```
- `admin_ids` → شناسهٔ عددی ادمین‌ها، مثل `[123456789]`

### ۴) آپلود روی هاست
- کل پوشهٔ `appleid-bot/` را آپلود کن.
- بهترین حالت: `config/` بیرون از `public_html` باشد. اگر نشد، همین `.htaccess`
  دسترسی وب به `config`, `src`, `cron`, `sql`, `lang`, `logs` را می‌بندد.
- فقط `webhook.php` باید از بیرون در دسترس باشد (HTTPS الزامی).

### ۵) ثبت Webhook
آدرس `webhook.php` را به بله بده. توکن مخفی به دو روش پشتیبانی می‌شود؛
مطمئن‌ترین راه برای بله، گذاشتن آن در **آدرس** است (پارامتر `?s=`):
```bash
curl "https://tapi.bale.ai/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://YOUR_DOMAIN/appleid-bot/webhook.php?s=<WEBHOOK_SECRET>"
```
> اگر پیام‌رسان هدر `secret_token` را پشتیبانی کند، آن هم بررسی می‌شود؛
> ولی روش `?s=` روی بله همیشه کار می‌کند. مقدار `s` باید **دقیقاً**
> برابر `webhook_secret` در config باشد.

### ۶) Cron (پاک‌سازی دادهٔ حساس)
در cPanel → Cron Jobs، هر ۶ ساعت:
```
php /home/USER/appleid-bot/cron/purge_sensitive.php
```

### ۷) تنظیم کارت واریز
در تلگرام به ربات (به‌عنوان ادمین) بفرست:
```
/setcard 6037xxxxxxxxxxxx نام صاحب کارت
```

### ۸) اتصال به سایت آیتکو (پورتال وب + لینک بله)
- در سایت: **phpMyAdmin → Import** فایل `database/appleid.sql` را اجرا کن.
- در پنل مدیریت سایت → **تنظیمات → اپل‌آیدی آمریکا**: «نمایش صفحهٔ اپل‌آیدی»
  را روشن کن، «یوزرنیم ربات بله» و «شروع قیمت‌ها» را پر کن.
- برای **پورتال وب** (ورود با موبایل، ثبت سفارش و تحویل در پروفایل که با همین
  ربات دیتابیس مشترک دارد):
  - اگر هنوز `web-portal.sql` را اجرا نکرده‌ای و دیتابیس از قبل ساخته شده،
    فایل `sql/web-portal.sql` را هم در همان دیتابیسِ ربات ایمپورت کن.
    (اگر `schema.sql` را تازه اجرا کرده‌ای، لازم نیست.)
  - در `config/config.local.php` سایت، مسیر پوشهٔ ربات را بده:
    ```php
    'appleid_bot_path' => '/home/USER/appleid-bot',
    ```
    سایت با خواندن همین مسیر، به دیتابیس/کلید رمز/کلاینت بله وصل می‌شود؛ پس
    سفارش‌های سایت هم داخل بله (همین ربات) مدیریت می‌شوند و کاربر تحویل را
    در پروفایلش می‌بیند.
- خروجی: صفحهٔ `https://YOUR_SITE/appleid` با «ورود/ثبت‌نام و ثبت سفارش»
  (پورتال وب) و دکمهٔ «سفارش در پیام‌رسان بله» (`ble.ir/<bot>?start=appleid`).

---

## دستورهای ادمین (داخل تلگرام)
| دستور | کار |
|------|-----|
| `/setcard <شماره> <نام>` | تنظیم کارت واریز |
| `/partners` | لیست همکارهای در انتظار تأیید (با دکمهٔ تأیید/رد) |
| `/addpartner <tg_id> <نام>` | افزودن همکار جدید (در انتظار) |
| `/ledger <partner_id>` | خلاصه‌حساب و تراکنش‌های همکار |
| `/settle <partner_id> <مبلغ>` | ثبت تسویهٔ همکار |
| `/admin` | راهنما |

تأیید/رد سفارش، «ثبت روی حساب همکار» و «ثبت اطلاعات نهایی» از روی دکمه‌های
زیرِ هر سفارش انجام می‌شود.

---

## نکات امنیتی
- همهٔ دادهٔ شخصی مشتری + کریدنشال نهایی با **AES-256-GCM** رمز می‌شود؛ کلید
  فقط در `config` است، نه دیتابیس.
- کد تأیید ایمیل **ephemeral** است: بعد از تحویل بلافاصله پاک می‌شود.
- دادهٔ حساسِ سفارش‌های تمام/رد/لغوشده بعد از `sensitive_data_retention_days`
  روز توسط cron پاک می‌شود.
- همهٔ کوئری‌ها Prepared Statement؛ همهٔ ورودی‌ها validate/sanitize.
- وب‌هوک با هدر مخفی محافظت می‌شود؛ فقط POSTِ معتبر پردازش می‌شود.
- هر اقدام مالی/مدیریتی در `audit_log` ثبت می‌شود.

## مدیریت محصول (MVP)
افزودن/ویرایش محصول و نوع ضمانت فعلاً از طریق دیتابیس (جدول‌های `products` و
`warranty_types`) انجام می‌شود. قیمت‌ها بر حسب تومان‌اند و `price_partner`
قیمتی است که به همکارهای تأییدشده نمایش داده می‌شود.
